# jobhunt configuration. Everything tunable lives here — no editing Python.
#
# Tune `filters` first. It is deterministic, free, and it is what keeps the
# LLM bill small: roughly 2000 raw postings -> 40 candidates before a single
# token is spent.
#
# Tuned for: Tanish Sanghvi — Full Stack / Backend / Frontend (React/Next.js) / AI web engineer
# Profile: new-grad/junior (0-2 YoE), React + Next.js + Node.js + Express + Flask + MongoDB + Firebase
# Target: India (Mumbai, Bangalore, Pune, Hyderabad, NCR / Remote), NO senior/staff/management

filters:
  # Title regexes, matched case-insensitively against the job title only.
  # A title must match at least one include AND zero excludes.
  #
  # Gotcha: "sde" as a bare regex does NOT match "Software Development
  # Engineer" — different words entirely. Use \bsde\b for the acronym and
  # list the spelled-out variants separately, as below.
  include_titles:
    # Core engineering titles
    - 'software engineer'
    - '\bsde\b'
    - '\bsde\s*-?\s*(i{1,3}|[123])\b'
    - 'software development engineer'
    - 'member of technical staff'
    - 'associate software engineer'
    - 'junior software engineer'
    - 'graduate engineer'

    # Backend / API / Full-Stack / Frontend / Web variants
    - '\b(backend|back-end|server|platform|api)\b.*\bengineer\b'
    - '\bengineer\b.*\b(backend|back-end|platform|api)\b'
    - 'full.?stack engineer'
    - 'full.?stack developer'
    - '\b(frontend|front-end|react|web)\b.*\bengineer\b'
    - '\bengineer\b.*\b(frontend|front-end|react|web)\b'
    - 'web developer'
    - 'web engineer'

    # Node.js / Python / JavaScript / TypeScript specific
    - '\b(node(\.js)?|python|javascript|typescript)\b.*\bengineer\b'
    - '\bengineer\b.*\b(node(\.js)?|python|javascript|typescript)\b'

    # AI/ML engineering (integrations/applications)
    - '\b(ai|ml|llm|genai|generative)\b.*\bengineer\b'
    - '\bengineer\b.*\b(ai|llm|genai|generative)\b'
    - 'ai application(s)? engineer'

  # Anything matching these is dropped even if an include matched.
  exclude_titles:
    - '\b(staff|principal|distinguished|fellow|architect)\b'   # too senior
    - '\bsenior\b'                                             # too senior (< 2 YoE)
    - '\b(director|vp|vice president|head of|chief|cto)\b'
    - '\b(manager|management|lead)\b'
    - '\b(intern|internship|apprentice|trainee)\b'             # too junior
    - '\b(sales|account executive|marketing|recruit|support|success)\b'
    - '\b(ios|android|mobile|game|designer|ux|ui)\b'
    - '\b(data scientist|machine learning scientist|research scientist)\b'
    - '\b(devops|devsecops)\b'
    - '\b(kernel|embedded|firmware|driver|packet|dataplane|data plane|dpdk)\b'

  # Substrings matched against "<location> <title>", case-insensitive.
  # Leave empty to accept every location.
  locations:
    - india
    - mumbai 
    - bengaluru
    - bangalore
    - pune 
    - thane 
    - hyderabad
    - gurugram
    - gurgaon
    - delhi 
    - chennai
    - noida
    - ahmedabad
    - surat
    
  # If true, anything reading as remote passes the location gate.
  allow_remote: true

  # Drop postings older than this. Set to 7 days for fresh weekly/daily runs.
  max_age_days: 28

# --- LLM stages -------------------------------------------------------------
screen_batch_size: 7      # jobs per screening call. Higher = cheaper, sloppier.
screen_jd_chars: 1400     # JD truncation for screening
draft_jd_chars: 7000      # JD truncation for drafting
score_threshold: 7.0      # 7.0 score bar: catches genuine matches while dropping poor fits
max_per_digest: 7         # hard cap on drafted kits per run

# --- paths ------------------------------------------------------------------
companies_file: companies.yaml
profile_file: profile.json
seen_file: seen.json
digest_file: out/digest.html
tracker_csv: out/tracker.csv
